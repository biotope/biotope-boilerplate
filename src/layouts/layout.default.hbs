{{bioInclude 'layouts/includes/htmltag' }}
<head>
	{{bioInclude 'layouts/includes/htmlhead.meta' }}

	<!-- inject:head:html -->
	<!-- endinject -->

	{{bioInclude 'layouts/includes/htmlhead.styles' }}
	{{bioInclude 'layouts/includes/htmlhead.scripts' }}
</head>
<body role="document" class="{{bodyClass}}">
    <main class="content">
        <article>
            {{bioInclude (lookup . 'contentMain') }}
        </article>
    </main>

	{{bioInclude 'layouts/includes/scripts' }}
</body>
</html>
