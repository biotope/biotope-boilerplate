---
# These values are used for creating the index
# It is also possible to use these inside the partials via frontMatter.name

title: Default
description: The default layout structure with some demo content

---

{{!-- DEV-Notes:
	specify the layout as the main partial
	inject the content via contentMain variable
--}}

{{> layouts/layout.default
	contentMain="scaffolding/demo.content"
}}
